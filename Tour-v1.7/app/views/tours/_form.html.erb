<%= simple_form_for(@tour, html: { class: 'form-horizontal'}) do |f| %>
  <% if @tour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tour.errors.count, "error") %> prohibited this tour from being saved:</h2>

      <ul>
      <% @tour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.input :title, label: 'Enter Title:', input_html: { class: 'form-control' }%>
  </div>
  
  <div class="form-group">
    <%= f.input :description, label: 'Description', input_html: { class: 'form-control' } %>
    </div>
  <div class="form-group">
    <%= f.input :location, label: 'Enter Tour Location:', input_html: { class: 'form-control' }%>
  </div>    
  <div class="form-group">
    <%= f.input :cost, label: 'Tour Cost:', input_html: { class: 'form-control' }%>
  </div>
  <div class="form-group">
     <%= f.input :image, as: :file %>
  </div>  

  <div class="actions">
    <br />
    <%= f.submit "Update Tour", class: "btn btn-info " %>
  </div>
<% end %>
